"use client";

import React from "react";

interface OuterWildlandsProps {
  cityRadius: number;
  themeIndex: number;
}

export default function OuterWildlands({ cityRadius, themeIndex }: OuterWildlandsProps) {
  return (
    <group name="outer-wildlands">
      {/* Stub component to prevent compilation errors */}
    </group>
  );
}
